# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'MobiusMobileWallet' do
  use_frameworks!
  pod 'Fabric'
  pod 'Crashlytics'

  target 'MobiusMobileWalletTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MobiusMobileWalletUITests' do
    inherit! :search_paths
    # Pods for testing
  end
end
